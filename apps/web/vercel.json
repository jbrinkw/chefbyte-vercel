{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/:path((?!api)(?!@vite)(?!@react-refresh)(?!.*\\.).*)",
      "destination": "/index.html"
    }
  ],
  "devCommand": "npm run dev -- --port $PORT --host"
}


